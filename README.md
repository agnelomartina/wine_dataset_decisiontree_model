since the datasets are categorical,decision tree classification model comes in handy.
The model is trained,tested and predicted
The metrics defined for classification such as accuracy score,precision,F_score,recall are determined.
The beauty of the classification report is that all the above metrics are measured automatically and a report is generated.
To visualise the dataset heatmaps and scatterplots are used to showcase the correlation of wine dataset and the alcohol vs color intensity
with the help of pickle file Json request and response is establised in the way of API so that when an input response is given,an output json response is recieved.
